#include<iostream>
using namespace std;
int main(){
     cout<<"Enter the Number : ";
     int x;
     cin>>x;
     (x % 2 == 0) ? cout<<"even" : cout<<"odd"; 
    // (x>=33) ? cout<<"pass" : cout<<"fail";  
}